# Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to git

# Database Configuration
DB_HOST=localhost
DB_USER=tryntmta_obuser
DB_PASSWORD=W__R-iut$@Ac&
DB_NAME=tryntmta_obportal

# Local Development
DEV_DB_HOST=localhost
DEV_DB_USER=root
DEV_DB_PASSWORD=root
DEV_DB_NAME=obrighton

# Application Settings
APP_DEBUG=false
APP_ENV=production
APP_URL=https://obrightonempire.com

# Session Configuration
SESSION_TIMEOUT=1800  # 30 minutes
SESSION_SECURE=true
SESSION_HTTPONLY=true
SESSION_SAMESITE=Lax

# Email Configuration
MAIL_HOST=mail.obrightonempire.com
MAIL_PORT=465
MAIL_USERNAME=no-reply@obrightonempire.com
MAIL_PASSWORD=your_email_password_here
MAIL_ENCRYPTION=ssl

# Security Settings
CSRF_TOKEN_LIFETIME=3600  # 1 hour
PASSWORD_HASH_ALGORITHM=PASSWORD_DEFAULT
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf,doc,docx,xlsx,csv
MAX_FILE_SIZE=5242880  # 5MB

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=warning
AUDIT_LOG_ENABLED=true
AUDIT_LOG_RETENTION_DAYS=90

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_LOGIN_ATTEMPTS=5
RATE_LIMIT_LOGIN_WINDOW=900  # 15 minutes

# Security
ENABLE_2FA=false
ENABLE_IP_WHITELIST=false
IP_WHITELIST=

# API Keys (if using external services)
RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=

# Backup Configuration
BACKUP_ENABLED=true
BACKUP_SCHEDULE=hourly
BACKUP_RETENTION_DAYS=30
